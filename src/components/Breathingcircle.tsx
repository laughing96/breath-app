interface Props {
    phase: string;
}

export function BreathingCircle(
    props: Props
) {
    return (
        <div style={{width:200, height:200, borderRadius:"50%", background:"#60a5fa",}}/>
    );
}
